package com.myitsolver.baseandroidapp.util

import com.myitsolver.baseandroidapp.BaseApplication
import com.myitsolver.baseandroidapp.models.CommonError

import retrofit2.Call
//RequestRunnerAndris().sendRequest(RetrofitMain().get().getUserById(getString(KEY_ID)),callback = {
//                    data = it
//                }, errorCallback = {
//                    toast(it.message)
//                })
//
//fun <T> Call<T>.send(showLoadingIndicator: Boolean = false, multipleRequest: Boolean = true, finished: (() -> Unit)? = null, error: ((CommonError) -> Unit)? = null, success: ((T) -> Unit)? = null){
//  BaseApplication.getAppBaseConfig().restConfig.requestRunner.sendRequest(this,showLoadingIndicator,multipleRequest = multipleRequest,finished = finished,error = error,success = success )
//}
//
